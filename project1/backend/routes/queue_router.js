const { Router } = require("express");
const { model } = require("mongoose");

queue_router = Router();

const {  }

queue_router.get("/", async function(req,res){
    const queue

})

module.exports={
    queue_router

}